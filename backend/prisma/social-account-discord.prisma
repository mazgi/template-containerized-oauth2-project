model SocialAccountDiscord {
  id         String   @id @default(cuid(2))
  providerId String   @unique
  email      String?
  userId     String   @unique
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt  DateTime @default(now())
}
